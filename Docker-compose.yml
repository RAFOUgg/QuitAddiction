version: '3.8'

services:
  quitaddictionbot:
    build: .
    container_name: quitaddictionbot
    restart: always
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
