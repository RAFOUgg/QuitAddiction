# Correct docker-compose.yml
version: '3.8'

services:
  quitaddictionbot:
    build: .
    container_name: quitaddictionbot
    restart: always
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./db:/app/db
      - ./logs:/app/logs
      - ./config:/app/config # This was a guess, ensure it's correct
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"